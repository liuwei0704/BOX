[TC:sh_exec, command=python3 -c "import os, json, re; base='/storage/emulated/0/VodPlus/LocalFiles/py_18'; files=[f for f in os.listdir(base) if f.endswith('.py') and f!='__init__.py']; data=[]; [data.append({'key':f.replace('.py',''), 'name':f.replace('.py',''), 'type':3, 'searchable':1, 'quickSearch':1, 'changeable':0, 'api':'https://gitlab.com/lemon52030/BOX/-/raw/main/py_18/'+f, '_lang':0 if not bool(re.search(r'[\u4e00-\u9fff]', f.replace('.py',''))) else 1}) for f in files]; data.sort(key=lambda x: (x['_lang'], len(x['name']), x['key'])); [d.pop('_lang', None) for d in data]; open('/storage/emulated/0/VodPlus/LocalFiles/tv_config_18.json','w',encoding='utf-8').write('[' + ',\n'.join([json.dumps(item, ensure_ascii=False, separators=(',',':')) for item in data]) + ']'); print('ok', len(data))"]
[/TC:sh_exec]