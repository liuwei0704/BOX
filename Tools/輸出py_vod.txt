[TC:sh_exec, command=python3 -c "import os, json, re; base='/storage/emulated/0/VodPlus/LocalFiles/py_vod'; files=[f for f in os.listdir(base) if f.endswith('.py') and f!='__init__.py']; short_keywords=['短剧','短劇','drama','微短剧','微短劇']; data=[{'key':f.replace('.py',''), 'name':('🔆' if any(kw in f.replace('.py','') for kw in short_keywords) else '❇️')+f.replace('.py','')+('[短劇]' if any(kw in f.replace('.py','') for kw in short_keywords) else '[影視]'), 'type':3, 'searchable':1 if any(kw in f.replace('.py','') for kw in short_keywords) else 0, 'quickSearch':1 if any(kw in f.replace('.py','') for kw in short_keywords) else 0, 'changeable':1 if any(kw in f.replace('.py','') for kw in short_keywords) else 0, 'api':'https://gitlab.com/lemon52030/BOX/-/raw/main/py_vod/'+f} for f in files]; data.sort(key=lambda x: (0, len(x['name']), x['name']) if x['searchable'] else (1, 0 if re.match(r'^[0-9]', x['name'].replace('🔆','').replace('❇️','').replace('[影視]','').replace('[短劇]','')) else 1 if re.match(r'^[A-Za-z]', x['name'].replace('🔆','').replace('❇️','').replace('[影視]','').replace('[短劇]','')) else 2, len(x['name'].replace('🔆','').replace('❇️','').replace('[影視]','').replace('[短劇]','')), x['name'])); open('/storage/emulated/0/VodPlus/LocalFiles/tv_config_vod.json','w',encoding='utf-8').write(',\n'.join([json.dumps(item, ensure_ascii=False) for item in data])); print('ok', len(data))"]
[/TC:sh_exec]